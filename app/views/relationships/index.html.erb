<br><br>
<h1>プロジェクト関係者</h1>
<p>管理者</p>
<% @users.each do |user| %>
  <% if user.roles == "管理者" %>
    <p>・<%= link_to(user.name,"/users/#{user.user_id}/edit") %></p> 
  <% end %>
<% end %>
<p>開発者</p>
<% @users.each do |user| %>
  <% if user.roles == "開発者" %>
    <p>・<%= link_to(user.name,"/users/#{user.user_id}/edit") %></p> 
  <% end %>
<% end %>